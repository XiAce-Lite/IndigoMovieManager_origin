name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore IndigoMovieManager.sln

      - name: Build
        run: dotnet build IndigoMovieManager.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test IndigoMovieManager.sln --configuration Release --no-build --verbosity normal
