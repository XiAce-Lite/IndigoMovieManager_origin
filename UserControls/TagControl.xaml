<UserControl
    x:Class="IndigoMovieManager.UserControls.TagControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="30"
    d:DesignWidth="100"
    mc:Ignorable="d">

    <Grid
        x:Name="TagGrid"
        PreviewKeyDown="TagGrid_PreviewKeyDown"
        PreviewKeyUp="TagGrid_PreviewKeyUp">
        <Border
            Height="Auto"
            Margin="4,4,0,0"
            Background="{DynamicResource ImmTagChipBackground}"
            BorderBrush="{DynamicResource ImmTagChipBackground}"
            BorderThickness="0"
            CornerRadius="4">
            <VirtualizingStackPanel
                Width="Auto"
                Orientation="Horizontal"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.IsDeferredScrollingEnabled="True"
                VirtualizingPanel.IsContainerVirtualizable="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">

                <TextBlock
                    x:Name="TagText"
                    Padding="4,1,4,1"
                    VerticalAlignment="Center"
                    Cursor="Hand"
                    FontSize="11"
                    FontStretch="UltraCondensed"
                    Foreground="{DynamicResource ImmTagChipForeground}"
                    MouseLeftButtonDown="TagText_MouseLeftButtonDown"
                    Text="{Binding}"
                    TextDecorations="Underline" />

                <TextBlock
                    x:Name="RemoveTagText"
                    Padding="4,1,4,1"
                    VerticalAlignment="Center"
                    Cursor="Hand"
                    FontSize="11"
                    FontStretch="UltraCondensed"
                    Foreground="{DynamicResource ImmTagChipForeground}"
                    MouseLeftButtonDown="RemoveTag_MouseLeftButtonDown"
                    Text="[X]" />
            </VirtualizingStackPanel>
        </Border>
    </Grid>
</UserControl>
